repos:
  - repo: local
    hooks:
      - id: dotnet-format
        name: dotnet format
        language: system
        entry: dotnet
        args: [format, src/TextServices.sln, --verify-no-changes, --severity, warn]
        pass_filenames: false
        files: \.cs$
